{
  "name": "codex-subscription-router-patcher",
  "private": true,
  "version": "0.1.0",
  "description": "Build tooling for the independent Codex Subscription Router macOS app patch",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/thewander02/codex-subscription-router.git"
  },
  "homepage": "https://github.com/thewander02/codex-subscription-router#readme",
  "bugs": {
    "url": "https://github.com/thewander02/codex-subscription-router/issues"
  },
  "engines": {
    "node": ">=22.12"
  },
  "scripts": {
    "check": "npm run check:go && npm run check:js && npm run check:python && npm run check:shell",
    "check:go": "go test ./... && go vet ./...",
    "check:js": "node --check ui/account-menu.js && node --check ui/thread-subscription.js && node --check ui/ui-test-bridge.cjs",
    "check:python": "python3 -m py_compile scripts/patch_app.py scripts/renderer_8109.py scripts/check_release.py && python3 -m unittest discover -s tests -v",
    "check:shell": "bash -n install.sh",
    "release:check": "python3 scripts/check_release.py",
    "precheck:python": "npm ci --ignore-scripts --no-audit --no-fund"
  },
  "devDependencies": {
    "@electron/asar": "4.2.1"
  }
}
